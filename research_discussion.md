# 上下楼场景落脚点规划研究方向讨论

## 核心思路

面向上下楼场景，输入深度图，分割每级台阶平面，引导机器人落脚点靠近台阶横向中心线，降低打滑/绊倒风险。

---

## 数据流架构

```
深度图 + 台阶中心线heatmap（双通道）
        │
        ▼
   CNN 编码器
        │
        ├──→ 辅助分割头（训练时）──→ 台阶 mask ──→ 生成 heatmap（推理时）
        │
        └──→ 特征 z ──→ Actor ──→ 动作
```

**关键设计决策：**
- 把编码器**特征**输入 Actor，而非分割结果（特征信息更丰富，梯度传播友好）
- 分割头作为**辅助监督**，强迫编码器学到台阶感知的表示，推理时可不运行
- 训练时 heatmap 来自 GT，推理时来自分割结果，接口一致

---

## 台阶中心线 Heatmap

用中心**线**替代中心**点**，原因：台阶横向自由度大，真正危险的是纵向太靠近边缘。

Heatmap 生成：与深度图同分辨率，台阶中心线处高值，远离衰减为零。

优势：
- 空间对齐：奖励信号与观测在同一像素空间，策略直接学"踩高值区域"
- 泛化性强：heatmap 随台阶几何变化，策略学通用规则
- 训练/推理接口统一

---

## 落脚点奖励设计

只在脚接触地面瞬间触发（利用已有 `contact_filt`），只惩罚**纵向**偏差：

```python
# d_longitudinal: 脚在行进方向上距台阶中心线的距离
r_footstep = exp(-d_longitudinal² / σ²)   # σ ≈ 0.1m
```

**观测与奖励对齐原则**：heatmap 输入 Actor + 奖励鼓励踩中心线，策略能明确理解动作目标。

---

## 训练流程（三阶段）

| 阶段 | 内容 | 目的 |
|------|------|------|
| 第一阶段 | GT 台阶中心线 + 落脚点奖励，无感知 | 验证控制侧可行性 |
| 第二阶段 | 加入 CNN 编码器 + 辅助分割头 | 训练感知表示 |
| 第三阶段 | 去掉 GT，只用感知特征 | 验证端到端性能 |

---

## 分割标注生成

仿真中台阶几何精确已知，可在 warp raycast 时记录每条射线打到的台阶面 ID，自动生成与深度图天然对齐的 per-pixel 标注，无需人工标注。

---

## 论文定位（目标：CoRL）

**主 claim（方向2）**：量化证明落脚点靠近中心线后，打滑/绊倒概率的下降幅度。

**技术贡献（方向3）**：辅助分割监督提升编码器表示质量，体现在策略性能上。

**可强化方向：**
- 泛化性：不只楼梯，扩展到任意结构化地面（台阶、石墩、窄梁），heatmap 作为统一接口
- 安全性量化：清晰的 metric，CoRL 友好
- 真机实验（H1 上下楼）：CoRL 必要条件

**与现有工作的区分点：**
需明确区别于 ETH ANYmal 系列、MIT Parkour、Berkeley DreamWaQ 等视觉引导落脚点工作，核心差异在于 heatmap 对齐机制 + 安全性量化。

---

## 待确认事项

- [ ] 真机实验条件（H1 硬件可用性）
- [ ] 仿真中台阶几何标注的具体实现方式
- [ ] 与哪些 baseline 对比
